<h1>Articles</h1>

<p>Our blog has <%= Article.public_count %> articles in it!</p>

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <a href="<%= article_path(article) %>">
          <%= link_to article.title, article %></li>
        </a>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
