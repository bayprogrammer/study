<h1>Hello from Rails!</h1>
<p>
  It is now <%= @time %>
</p>
<p>
  Time to say
  <%= link_to "Goodbye", say_goodbye_path %>!
</p>
<h2>A fine selection of expressions</h2>
<dl>
  <dt>Addition (<code>1 + 2</code>):</dt>
  <dd><%= 1 + 2 %></dd>
  <dt>Concatenation (<code>"cow" + "boy"</code>):</dt>
  <dd><%= "cow" + "boy" %></dd>
  <dt>Time in one hour (<code>1.hour.from_now.localtime</code>):</dt>
  <dd><%= 1.hour.from_now.localtime %></dd>
</dl>
